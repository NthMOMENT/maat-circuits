[package]
name = "host"
version = "0.1.0"
edition = "2021"

[dependencies]
methods = { path = "../methods" }
risc0-zkvm = { version = "^3.0.6", features = ["cuda"] }
serde = { version = "1.0", features = ["derive"] }
anyhow = "1.0"
